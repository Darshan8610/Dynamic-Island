<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Platform theme: a minimal dark launch backdrop. The real theme is applied in Compose
      (core/ui/theme) so that the same tokens drive both the app UI and the overlay island.
    -->
    <style name="Theme.Island" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/island_background</item>
        <item name="android:colorBackground">@color/island_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
